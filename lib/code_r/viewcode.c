void printLine(LINE *para, int max_X, int max_Y)
{
	if(strlen(para->text)+1 > max_X) // if the length of character over extent
	{
	}
	if(n_of_line(para) > max_Y) // if the line of paragraph over extent
	{
	}
}
